---
import Base from '../../layouts/Base.astro';
import HomePage from '../../components/pages/HomePage.astro';
import { t } from '../../i18n';

const lang = 'ar' as const;
const copy = t(lang);
---

<Base lang={lang} route="home" title={copy.home.seo.title} description={copy.home.seo.description} flush>
  <HomePage lang={lang} />
</Base>
