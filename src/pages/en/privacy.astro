---
import Base from '../../layouts/Base.astro';
import LegalPage from '../../components/pages/LegalPage.astro';
import { t } from '../../i18n';

const lang = 'en' as const;
const copy = t(lang);
---

<Base lang={lang} route="privacy" title={copy.privacy.seo.title} description={copy.privacy.seo.description}>
  <LegalPage lang={lang} doc="privacy" />
</Base>
