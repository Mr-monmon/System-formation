---
import Base from '../../layouts/Base.astro';
import LegalPage from '../../components/pages/LegalPage.astro';
import { t } from '../../i18n';

const lang = 'en' as const;
const copy = t(lang);
---

<Base lang={lang} route="terms" title={copy.terms.seo.title} description={copy.terms.seo.description}>
  <LegalPage lang={lang} doc="terms" />
</Base>
